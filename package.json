{
  "name": "nouveaumonde",
  "version": "0.1.0",
  "dependencies": {},
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "concurrently": "^3.6.1",
    "node-sass": "^4.9.4"
  },
  "scripts": {
    "sass": "node-sass web/themes/monde/scss/style.scss --output web/themes/monde/css",
    "sass:watch": "node-sass web/themes/monde/scss/style.scss --watch --output web/themes/monde/css",
    "start": "npm run sass:watch",
    "build": "npm run sass",
    "deploy": "./deploy.sh production deploy",
    "deploy-dry-run": "./deploy.sh production"
  }
}
